<h1><%= @item.name %></h1>

<p>
<%= image_tag @item.avatar.url(:medium) %><br />
<%= @item.description %>
</p>

<% if user_signed_in? %>
<%= link_to "Like", pins_path(:user_id => current_user.id, :item_id => @item.id), :method => :post %>
<% end %>